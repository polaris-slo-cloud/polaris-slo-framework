
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.6.1
  creationTimestamp: null
  name: costefficiencymetricmappings.metrics.polaris-slo-cloud.github.io
spec:
  group: metrics.polaris-slo-cloud.github.io
  names:
    kind: CostEfficiencyMetricMapping
    listKind: CostEfficiencyMetricMappingList
    plural: costefficiencymetricmappings
    singular: costefficiencymetricmapping
  scope: Namespaced
  versions:
  - name: v1
    schema:
      openAPIV3Schema:
        description: CostEfficiencyMetricMapping is the Schema for the costefficiencymetricmappings
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: CostEfficiencyMetricMappingSpec defines the desired state
              of CostEfficiencyMetricMapping
            properties:
              metricConfig:
                description: The cost efficiency configuration.
                properties:
                  targetThreshold:
                    description: "The target threshold for the `performance` part
                      of the cost efficiency equation. \n Depending on the specific
                      cost efficiency metric implementation, the threshold may be
                      considered as a lower bound (`performance` samples should be
                      above the threshold) or as an upper bound (`performance` samples
                      should be below the threshold). \n If the implementation of
                      the metric relies on a bucketing mechanism (e.g., Prometheus
                      histograms), the set of allowed values may be limited."
                    format: int64
                    type: integer
                required:
                - targetThreshold
                type: object
              targetRef:
                description: Specifies the target for which the metric should be computed.
                properties:
                  apiVersion:
                    description: API version of the referent
                    type: string
                  kind:
                    description: 'Kind of the referent; More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds"'
                    type: string
                  name:
                    description: 'Name of the referent; More info: http://kubernetes.io/docs/user-guide/identifiers#names'
                    type: string
                required:
                - kind
                - name
                type: object
            required:
            - metricConfig
            - targetRef
            type: object
          status:
            description: CostEfficiencyMetricMappingStatus defines the observed state
              of CostEfficiencyMetricMapping
            type: object
        type: object
    served: true
    storage: true
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
