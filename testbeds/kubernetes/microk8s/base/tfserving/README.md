# TF-Serving deployment for Predicted Metric Container

This directory contains files to deploy TF-Serving as service in Kubernetes, which serves as inference endpoint for the
Predicted Metric Controller.


