import { ObjectKind } from './object-kind';

/**
 * Identifies an elastcitity strategy kind/type.
 */
export class ElasticityStrategyKind extends ObjectKind {

    constructor(initData?: Partial<ElasticityStrategyKind>) {
        super(initData);
    }

}
