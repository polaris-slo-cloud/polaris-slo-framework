
/**
 * A SlocTransformer is used to convert between orchestrator-independent `@SlocTransformable` objects
 * and orchestrator-specific plain objects that be serialized directly.
 */
export interface SlocTransformer<T> {

}
