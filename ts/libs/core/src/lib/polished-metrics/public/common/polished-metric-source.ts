
/**
 * A `PolishedMetricSource` is used to obtain a complex, processed metric from a deployment.
 */
export interface PolishedMetricSource {
    // ToDo
    // We need parameters to specify the deployment
}
