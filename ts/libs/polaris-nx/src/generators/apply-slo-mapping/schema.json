{
    "$schema": "http://json-schema.org/schema",
    "cli": "nx",
    "title": "Apply SLO Mapping",
    "description": "Apply an SLO Mapping instance to the configured Kubernetes instance",
    "type": "object",
    "properties": {
        "sloMappingTsPath": {
            "type": "string",
            "description": "",
            "$default": {
                "$source": "argv",
                "index": 0
            },
            "x-prompt": "Please enter the path of the SLO Mapping's .ts file within the slo-mappings directory."
        }
    },
    "required": ["sloMappingTsPath"]
}
