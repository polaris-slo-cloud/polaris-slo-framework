import { mappingsCommonMappings } from './mappings-common-mappings';

describe('mappingsCommonMappings', () => {
    it('should work', () => {
        expect(mappingsCommonMappings()).toEqual('mappings-common-mappings');
    })
})