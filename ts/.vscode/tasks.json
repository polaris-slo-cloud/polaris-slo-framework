{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-cli-polaris-k8s-serializer",
            "type": "shell",
            "command": "npx nx build cli-polaris-k8s-serializer --with-deps=true --source-map=true"
        },
        {
            "label": "build-cost-efficiency-slo-controller",
            "type": "shell",
            "command": "npx nx build slo-cost-efficiency-slo-controller --with-deps=true --source-map=true"
        },
        {
            "label": "build-horizontal-elasticity-strategy-controller",
            "type": "shell",
            "command": "npx nx build elasticity-horizontal-elasticity-strategy-controller --with-deps=true --source-map=true"
        },
        {
            "label": "build-vertical-elasticity-strategy-controller",
            "type": "shell",
            "command": "npx nx build elasticity-vertical-elasticity-strategy-controller --with-deps=true --source-map=true"
        }
    ]
}
